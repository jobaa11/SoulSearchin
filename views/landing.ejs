<%- include('./partials/header') %>

    <% if (user && profile) { %>

        <h1>Welcome To SoulSearch.</h1>
        
        <% } else { %>
            <h1>Welcome To SoulSearch.</h1>
             <a href="">CREATE ACCOUNT</a>
            <% } %>
            <%- include('./partials/footer') %>