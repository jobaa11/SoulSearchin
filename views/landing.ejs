<%- include('./partials/header') %>

    <% if (user && profile) { %>

        <h1 id="welcome">Welcome To SoulSearch.</h1>
        
        <% } else { %>
            <h1>Welcome To SoulSearch.</h1>
             <a class="landing" href="/auth/google"><button>CREATE ACCOUNT</button></a>
            <% } %>
            <%- include('./partials/footer') %>