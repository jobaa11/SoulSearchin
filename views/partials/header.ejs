<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inconsolata:wght@200&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <title>SoulSearch</title>
</head>

<body>
  <nav>
    <div>
      <a href="/">SoulSearch</a>
      <ul>
        <% if (user) { %>
          <% if (profile && !profile.isInstructor) { %>
            <a href="/about">ABOUT US</a>
            <a href="/contact">CONTACT</a>
            <a href="/profiles/student/home">VIEW PROFILE</a>

            <% } else if (profile && profile.isInstructor) { %>
              <a href="/about">ABOUT US</a>
              <a href="/contact">CONTACT</a>
              <a href="/profiles/instructor/home">VIEW PROFILE</a>
              <a href="/instruments">EDIT INSTRUMENTS</a>
              <% } else { %>
                <% } %>
                  <% } %>
                    <% if (!user) { %>
                      <a href="/about">ABOUT US</a>
                      <a href="/contact">CONTACT</a>
                      <% } %>
      </ul>
    </div>
    <div>
    </div>
  </nav>
  <main>